<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Blog{% endblock %}</title>

    <!-- Bootstrap 5 CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    {% block stylesheets %}
    <style>
        .categories a {
            margin-right: 10px;
            padding: 5px 10px;
            text-decoration: none;
            color: #333;
        }

        .categories a.active {
            font-weight: bold;        /* pogrubienie */
            color: #007bff;           /* niebieski */
            background-color: #e0f0ff; /* lekkie podświetlenie */
            border-radius: 4px;
        }

        .posts-list article {
            margin-bottom: 30px; /* odstęp między postami */
        }

        h1.page-title {
            margin-bottom: 40px; /* odstęp pod nagłówkiem */
        }
    </style>
    {% endblock %}
</head>

<body>
    <header class="bg-primary text-white py-3 mb-4">
        <div class="container">
            <h1 class="mb-0">Blog</h1>
        </div>
    </header>

    <main class="container">
        {% block body %}{% endblock %}
    </main>

    <footer class="container my-4">
        <a href="{{ path('homepage') }}" class="btn btn-secondary">Przenieś do strony głównej</a>
    </footer>

    <!-- Bootstrap 5 JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
